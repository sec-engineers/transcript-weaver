# TODO

- Implement `trwclean` with a small Gemini provider boundary and strict packet preservation.
- Specify output-rules schema only when implementing `trwout`.
- Implement atomic Markdown writers against disposable fixture vaults.
- Design source deletion separately; require verified persistence before enabling it.
- Add optional arbitrary-recording selection only after the newest-recording workflow is stable.
